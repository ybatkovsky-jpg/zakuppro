# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | S03 implementation | architecture | FastAPI router organization | Modular routers per entity with dedicated router files | Keeps code organized as API grows; each entity has its own router file (e.g., projects.py) with standard CRUD endpoints; main.py includes all routers. This pattern scales better than monolithic routers and makes testing easier. | false | agent |
| D002 | M002 planning | architecture | Docker Service Structure for M002 | 4 отдельных Docker сервиса: fastapi, celery-worker, telegram-bot, rabbitmq | Isolation: падение worker'а не останавливает API и Bot. Scaling: можно запустить несколько worker'ов для параллельной обработки. Restart policies: разные настройки для разных типов сервисов. | yes | collaborative |
| D003 | M002 planning | architecture | LLM Error Handling Strategy | Retry 2x с exponential backoff (1s, 5s) → DLQ с сохранением контекста → Telegram alert | Retry handles temporary API failures. DLQ гарантирует что ничего не теряется. Alert даёт владельцу visibility на проблемы. | yes | collaborative |
| D004 | M002 planning | library | Telegram Bot Framework | python-telegram-bot | Зрелая библиотека с хорошей документацией. Поддержка webhook и long polling. Легкая интеграция с RabbitMQ. | yes | agent |
| D005 | M002 planning | architecture | DLQ Persistence Strategy | RabbitMQ DLQ + отдельная таблица в БД для failed tasks с контекстом | RabbitMQ DLQ из коробки для retry logic. БД таблица для персистентности и возможности перезапустить через UI later. Контекст нужен для debugging. | yes | agent |
| D006 | M002 planning | architecture | Excel Parsing Strategy | pandas для чтения + OpenAI GPT-4o для распознавания структуры dirty таблиц | pandas reliably читает Excel. LLM нужна для dirty таблиц (объединенные ячейки, многоэтажные шапки). GPT-4o недорогой и отлично работает со структурированным текстом. | yes | agent |
| D007 | S01-T03 implementation | architecture | Celery worker health check approach | Use app.control.inspect().ping() with 2s timeout | Standard Celery pattern for checking worker availability. Timeout prevents blocking. Returns empty dict when no workers, enabling graceful degradation detection. | yes | agent |
| D008 | S01-T03 implementation | architecture | RabbitMQ health check method | Use app.connection_or_acquire() for broker connectivity | connection_or_acquire() is thread-safe and properly handles connection lifecycle. Returns connection context that auto-closes, preventing resource leaks. | yes | agent |
| D009 | S01-T03 implementation | architecture | Health check failure behavior | Return 503 HTTP status when ANY service is degraded | Fail-fast approach ensures load balancers stop routing to unhealthy instances. Clear distinction between healthy (200) and degraded (503) states. | yes | agent |
| D010 | S02 planning - choosing polling mode for python-telegram-bot | architecture | Telegram Bot Communication Mode | Long polling (run_polling) instead of webhook | Webhook requires HTTPS endpoint and public URL; long polling works in Docker without additional infrastructure. Can migrate to webhook later if needed for scalability. | yes | agent |
| D011 | M002-S03 Excel Parsing + AI-Agent | architecture | Excel parsing strategy for dirty invoice tables | pandas + OpenAI GPT-4o | pandas reliably reads Excel files with openpyxl engine. LLM is required for dirty tables with merged cells, multi-line headers, and inconsistent Russian column names. GPT-4o is cost-effective and excels at structured table recognition. | true | agent |
| D012 | M002-S03 Excel Parsing + AI-Agent | architecture | AI extraction output format for BOM data | Structured JSON via OpenAI JSON Schema (strict mode) | JSON Schema strict mode guarantees 100% valid output. Eliminates parsing errors and downstream validation complexity. Direct mapping to Pydantic models for type safety. | true | agent |
| D013 | M002-S03 Excel Parsing + AI-Agent | operability | Retry strategy for OpenAI API rate limits | Exponential backoff: 1s, 2s, 4s with max_retries=2 | RateLimitError and APITimeoutError are transient. Exponential backoff respects API limits while recovering quickly. After 3 total attempts (1 initial + 2 retries), task moves to DLQ for manual inspection. | true | agent |
| D014 | S04 T02 - Supplier auto-creation with placeholder emails | library | Python-slugify for email generation from supplier names | Use python-slugify library for safe email local-part generation from Russian company names | Handles Russian transliteration, special characters, and edge cases (e.g., "ООО Вектор" → "ooo-vektor"). Safer than manual string manipulation. | yes | agent |
| D015 | S04 T02 - Supplier Resolver error handling | pattern | Supplier resolver returns None for empty names | Return None instead of raising exception for empty/whitespace supplier names | Allows graceful handling in downstream code. Task orchestration can skip items with missing supplier names without failing entire BOM processing. | yes | agent |
| D016 | S04 T03 - Telegram notification error handling | pattern | Telegram notifier returns bool on failure | Return False instead of raising exceptions when Telegram API calls fail | Celery tasks should continue processing even if notifications fail. Failed notifications are non-critical vs data persistence. | yes | agent |
| D017 | S04 T04 - Celery task error isolation | pattern | Deferred imports inside Celery task try block | Import database, models, and helper modules inside task try block rather than at module level | Avoids startup errors when dependencies are unavailable. Allows graceful degradation and optional feature support (e.g., telegram_notifier). | yes | agent |
| D018 | S04 T04 - Celery task chaining vs blocking | pattern | Blocking call with .apply() within orchestration task | Use parse_excel_bom.apply().get() for blocking execution inside process_bom_to_project | Orchestration task needs result immediately for database operations. Chaining would require passing large Excel data between tasks. | yes | agent |
| D019 | M003 S01 database schema design | architecture | Invoice File Storage Strategy | Store original invoice files as BLOB in PostgreSQL BYTEA column | Audit trail requires original files for legal protection and re-parsing when extraction logic changes. Database storage simplifies backup/restore and keeps transactional consistency. PostgreSQL BYTEA handles files up to 1GB. | yes — can migrate to S3/external storage if database grows too large | agent |
| D020 | M003 S01 llm_provider.py design | architecture | LLM Provider Fallback Strategy | Provider-agnostic wrapper with configuration-driven primary/secondary providers and automatic fallback on rate limit/timeout | Different providers have different pricing (GPT-4o-mini cheap, Gemini 2.5 Flash cheaper, Claude Sonnet premium). Fallback ensures continuity when primary API fails. Configuration allows switching without code changes. | yes — can add more providers or change fallback logic | agent |
| D021 | M003 S02 email_worker.py design | integration | IMAP Ingestion Strategy | Auto-poll separate email mailbox (invoices@company.com) with Celery task publishing for attachment processing | Suppliers send invoices in response to purchase requests. 1 email = 1 supplier = 1 invoice pattern simplifies processing. Auto-poll balances real-time processing with load control. Celery integration reuses existing RabbitMQ infrastructure. | yes — can switch to IMAP IDLE for real-time if needed | agent |
| D022 | M003 S04 invoice_verifier.py design | architecture | Invoice Verification Matching Strategy | Multi-tier matching: exact SKU match → OK, SKU differs + RapidFuzz name similarity >85% → clarification, quantity differs → partial flag | Strict matching fails on legitimate name variations. Fuzzy matching reduces false positives while catching discrepancies. Clarification flow handles ambiguous cases. Similarity threshold tunable based on real data. | yes — threshold can be adjusted based on production data | agent |
| D023 | M003 S05 notifications design | integration | Email Notification Strategy for Clarifications | SMTP outbound for supplier clarifications, Telegram for owner notifications | Suppliers already use email for invoices. Email clarifications fit existing workflow. Telegram provides real-time alerts for owner. Separation of concerns: supplier comms via email, internal comms via Telegram. | yes — can add web UI for clarifications in M005 | agent |
| D024 |  | architecture | Bank Statement Parser design pattern | Follow invoice_parser.py pattern: class-based parser with factory function and convenience function, returning dict with status='success'\|'error' for error handling consistency | Codebase has established parser service pattern in invoice_parser.py. Reusing this pattern maintains consistency, reduces cognitive load, and allows S03 Celery task to handle bank statements the same way as invoices (error handling, logging, retry logic). | Yes | agent |
| D025 | Planning S05 Transaction Matching API | architecture | TransactionMatchingAudit unified audit trail for manual and auto matches | Add unresolved_transaction_id nullable FK to TransactionMatchingAudit model | Unified audit trail across auto-match (BankTransaction) and manual-match (UnresolvedTransaction) workflows. Single query retrieves all matching history. Backward compatible (NULL for existing auto-match records). Alternative separate UnresolvedTransactionAudit table would split audit history. | no | agent |
| D026 | Planning S05 Transaction Matching API | architecture | Bulk match transaction safety strategy | Database transaction with rollback on any failure | Partial success in bulk operations creates inconsistent state. Either all matches succeed or none do. Implementation wraps bulk match in db.begin_nested() context, rollback on exception. | yes | agent |
| D027 | Planning S05 Transaction Matching API | architecture | Candidate suggestion tolerances for manual matching | 10% amount tolerance, 90 days date window (wider than auto-match) | Manual review allows human judgment; show more candidates even if confidence is lower. Auto-match uses 5%/30 days for strict automation, while manual matching needs broader candidate set for accountant review. | yes | agent |
